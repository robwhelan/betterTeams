<%= form_for(@survey_score) do |f| %>
  <% if @survey_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_score.errors.count, "error") %> prohibited this survey_score from being saved:</h2>

      <ul>
      <% @survey_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comms_question_id %><br>
    <%= f.text_field :comms_question_id %>
  </div>
  <div class="field">
    <%= f.label :comms_suvey_id %><br>
    <%= f.text_field :comms_suvey_id %>
  </div>
  <div class="field">
    <%= f.label :score %><br>
    <%= f.number_field :score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
