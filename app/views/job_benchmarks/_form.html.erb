<%= form_for(@job_benchmark) do |f| %>
  <% if @job_benchmark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_benchmark.errors.count, "error") %> prohibited this job_benchmark from being saved:</h2>

      <ul>
      <% @job_benchmark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
